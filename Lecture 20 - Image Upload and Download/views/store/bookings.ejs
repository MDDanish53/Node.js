<%- include('../partials/head') %>
<style>
    @keyframes bounce-in {
      0% { opacity: 0; transform: scale(0.8);}
      60% { opacity: 1; transform: scale(1.05);}
      100% { opacity: 1; transform: scale(1);}
    }
    .animate-bounce-in {
      animation: bounce-in 0.7s cubic-bezier(.4,0,.2,1) both;
    }
  </style>
  </head>
  <body>
     <%- include('../partials/header') %>
     <div class="flex items-center justify-center h-[100vh]">
     <div class="bg-white/80 rounded-3xl shadow-2xl p-10 max-w-md w-full flex flex-col items-center animate-bounce-in">
    <h1 class="text-4xl font-extrabold text-red-600 mb-2 text-center">My Bookings</h1>
    <p class="text-gray-500 mb-6 text-center">Bookings will appear here</p>
  </div>
</div>
  </body>
</html>
